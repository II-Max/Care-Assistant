# ============================================
# 🏥 AI Customer Care Assistant — Environment
# Bệnh viện Tim Hà Nội
# ============================================

# === NVIDIA NIM API ===
# API Key cho NVIDIA NIM (bắt buộc)
NVIDIA_API_KEY=nvapi-your-key-here

# Model cho chat (optional, default: meta/llama-3.1-70b-instruct)
NVIDIA_MODEL=meta/llama-3.1-70b-instruct

# Model cho embedding (optional, default: nvidia/nv-embedqa-e5-v5)
NVIDIA_EMBED_MODEL=nvidia/nv-embedqa-e5-v5

# === Server ===
AI_SERVICE_HOST=0.0.0.0
AI_SERVICE_PORT=8001
ALLOWED_ORIGINS=http://localhost:8001,http://127.0.0.1:8001
CHAT_RATE_LIMIT_PER_MINUTE=10

# === RAG Pipeline ===
CHUNK_SIZE=400
CHUNK_OVERLAP=50
TOP_K=5
SIMILARITY_THRESHOLD=0.35

# === Database (PostgreSQL — optional, fallback SQLite) ===
DB_HOST=localhost
DB_PORT=5432
DB_NAME=care_assistant
DB_USER=app_user
DB_PASSWORD=

# === Firebase (optional — offline mode if not configured) ===
FIREBASE_CREDENTIALS_PATH=

# === Notification ===
# SMS API Provider (optional — placeholder in Phase 3)
SMS_API_URL=
SMS_API_KEY=
# Zalo OA (optional — placeholder in Phase 3)
ZALO_OA_TOKEN=
# Email (optional — placeholder in Phase 3)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=cskh@timhanoi.vn

# === Frontend URLs ===
FRONTEND_URL=http://localhost:8000
